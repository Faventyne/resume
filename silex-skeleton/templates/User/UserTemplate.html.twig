{% extends "layout.html.twig" %}

{% block content %}
    {% if myVar is not defined %}
        {% set myVar = "hello world" %}
    {% endif %}
    {{ parent() }}
    <ul>
        {% for user in users %}
        <li>
            {{ user.getId() }} :
            {{ user.getfname()|upper }}
            {{ user.getlname() }}
            <?php
            </li>
            {% endfor %}
    </ul>

{% endblock %}